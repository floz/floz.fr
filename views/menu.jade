include mixins

-var entries = [ { url: '/', id: "home", name: "HOME" } ]

nav#menu
	-if( navId == "home" )
		#bt_home.activated
			mixin bt( "HOME", "/" )
	-else 
		#bt_home
			mixin bt( "HOME", "/" )